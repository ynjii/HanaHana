using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class Spike1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void FixedUpdate()
    {
        //지형체크
        Vector2 frontVec = New Vector2(transform.position.x + transform.position, transform.position.y);
        Debug.DrawRay(transform.position, Vector3.down, new Color(0,1,0));
        RaycastHit2d rayhit = Physics.Raycast(frontVec, Vector3.down, 1, LayerMask.GetMask("Platform"));
        if(rayHit.collider == null){
            debug.Log("앞이 낭떨어지 맞음");
        }
    }
}
*/
